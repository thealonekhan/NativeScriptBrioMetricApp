# Enable AndroidX
android.useAndroidX=true
android.enableJetifier=true

# NDK version (match with installed version)
android.ndkVersion=29.0.13113456

# Build configuration
org.gradle.daemon=true
org.gradle.parallel=true